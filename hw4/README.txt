The S2S model is as same as the Best model.
And we don't completely successfully build RL model.
However we still need to input three arguments, 
as in the follwing example:

./run.sh S2S input.txt output.txt
